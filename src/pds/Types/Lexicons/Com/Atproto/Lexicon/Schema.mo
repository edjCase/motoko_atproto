import Json "mo:json";

module {
    // com.atproto.lexicon.schema
    // Representation of Lexicon schemas themselves, when published as atproto records.

    public type Record = {
        lexicon : Int;
    };

};
